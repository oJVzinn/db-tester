services:
  db:
    image: mariadb
    restart: no
    environment:
      MARIADB_ROOT_PASSWORD: admin
  phpmyadmin:
    image: phpmyadmin
    restart: no
    ports:
      - 8080:80
    environment:
      - PMA_ARBITRARY=1
